<%= link_to(item_path(item)) do %>
  <div class="card">    
      <%= image_tag(photo_url) %>
      <div class="card__intro">
        <h4 class="card__title"><%= card_title %></h4>
      </div>

  </div>
<% end %>
