<%= form_tag(add_path, class: 'wide--center', method: :get, remote: true) do %>

  <%= hidden_field_tag('item', item.id)%>
  <%= button_tag('AJOUTER AU PANIER', class:  'card__add') do %>
    <div class="hidden">
      <%= item.id %>
    </div>
    <div>AJOUTER AU PANIER</div>
    <svg>
      <use href="#pluss"></use>
    </svg>
  <% end %>

<% end %>
